[pytest]
testpaths = tests
addopts = -m "not integration"
markers =
    integration: needs the cached embedding model and real vectors. Excluded by default
        so the standard suite stays offline and fast. Run with: pytest -m integration
